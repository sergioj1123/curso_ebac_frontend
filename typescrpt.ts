function multiply (a: number, b: number): number{
    return a*b;
}

function sawHello (name: string): string {
    return `Olá ${name}`;
}

console.log(multiply(10, 2));
console.log(sawHello("Sérgio"));